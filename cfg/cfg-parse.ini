[global]
foo=global var

[section1]
bar=$foo + section 1 var
baz=$((1+2))
quz=uname $(uname -a)
include cfg-parse-inc.ini

[section2]
bar=$foo + section 2 var
baz=$((11+22))
quz=curent id: $(id -un)
include cfg-parse-inc2.ini

